name: Build & Validate

on: push

jobs:
  install:
    name: Install
    runs-on: ubuntu-latest
    steps:
      - name: Clone
        uses: actions/checkout@v1
      - name: Install node
        uses: actions/setup-node@v1
        with:
          node-version: 10.x
      - name: Debug
        run: ls -al
      - name: Install dependencies
        run: npm install
  build:
    name: Validate
    runs-on: ubuntu-latest
    needs: install
    steps:
      - name: Test
        run: npm run test
      - name: Lint
        run: npm run lint
  validate:
    name: Build
    runs-on: ubuntu-latest
    needs: install
    steps:
      - name: Build
        run: npm run build
